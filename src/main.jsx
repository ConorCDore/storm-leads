import React from "react";
import ReactDOM from "react-dom/client";
import StormLeads from "./StormLeads.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StormLeads />
  </React.StrictMode>
);
